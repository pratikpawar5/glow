import { FeedBackMeesagePipe } from './feed-back-meesage.pipe';

describe('FeedBackMeesagePipe', () => {
  it('create an instance', () => {
    const pipe = new FeedBackMeesagePipe();
    expect(pipe).toBeTruthy();
  });
});
